package com.company;

public class ArrayMatcher {

    /**
     *  check if 2d shape in A array will fit in new array B
     *  Assuming that 2d arays are square
     * @.pre  A.length>0 && A.[A.length]>0 && B.length>0 && B.[B.length]>0 && A != null && B != null
     *x and y are uppder left corner coordination of shape coordination in Array A
     * @.post RESULT == FORALL(x,y ; x >= 0 && y >= 0; x < B.length && y < B.[B.length])
     *
     */


    public boolean willShapeFitInArray(int[][] A, int[][] B) {
    }
}
